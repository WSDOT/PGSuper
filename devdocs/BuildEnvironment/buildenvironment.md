# Build Environment Setup {#build_environment_setup}

Before PGSuper/PGSplice can be built, a developer's machine needs a few one-time environment prerequisites in place - environment variables, third-party SDKs, and similar setup that isn't part of the source tree itself. The following topics document these prerequisites.

@subpage grid_props_environment_variable
